dist/
node_modules/
*.log

# Generated by `npm run store:screenshots`. Not committed: they are screenshots
# of real threads, so they name two real accounts and show that those accounts
# block each other. That is fine to submit to a store listing deliberately, and
# needlessly permanent to bake into git history. Regenerate when a listing needs
# them. (Same reasoning as the anonymized test fixtures.)
store/screenshots/
